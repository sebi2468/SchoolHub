/*
const pageTitle = document.querySelector('.page-title');
const currentPage = window.location.pathname;

if (currentPage === "/index.html") {
  pageTitle.children[0].innerHTML = "Overview".toUpperCase();
} else {
  pageTitle = currentPage;
}
*/